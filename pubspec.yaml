name: ResQMove
description: ResQmove Ambulance Service Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  iconsax: ^0.0.8
  flutter_map: ^7.0.2
  latlong2: ^0.9.0
  http: ^1.2.1
  geolocator: ^13.0.0
  shared_preferences: ^2.2.3
  share_plus: ^10.1.4
  url_launcher: ^6.3.1
  connectivity_plus: ^6.1.4
  socket_io_client: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_native_splash: ^2.4.4

flutter:
  uses-material-design: true

flutter_native_splash:
  color: "#0D1B2A"
  android_12:
    color: "#0D1B2A"
